global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'm365-dashboard'
    static_configs:
      - targets:
          # Replace with your running instances host:port
          - 'host.docker.internal:9000'
          - 'host.docker.internal:9000'
          - 'host.docker.internal:10000'
