plan_id: "plan:m365-cps-trkC-p2-agents-yaml-schema"
parent_plan_id: "plan:m365-cps-trkC-truth-in-advertising"
title: Parent Phase C2 — agents.yaml Schema Upgrade
version: 1.0
status: draft
owner: Phil Siniscalchi (Founder/Owner)
date_created: 2026-05-01
date_updated: 2026-05-01
north_star_ref: docs/NORTH_STAR.md
execution_plan_ref: docs/platform/EXECUTION_PLAN.md - M365 CPS Track C P2
domain: framework
math_algorithm_scope: false
north_star_alignment:
  source: docs/NORTH_STAR.md
  principles:
    - Gates-by-construction integrity (denials must be honest)
    - Tool-first determinism (capability surface must equal advertised surface)
    - "Fail-closed semantics (unknown action != blocked write)"
    - Operator observability (the system must explain its own state)
    - Plan-first execution with strict per-phase gates
  anti_alignment:
    - Mutation fence on actual write actions stays on
    - Risk-tier classification per agent stays unchanged
    - Audit envelope format stays unchanged
    - Two-phase confirmation gate for high-risk mutations stays on
intent_capture:
  user_stated_requirements:
    - Add coverage_status to agents.yaml entries; migrate existing data; update validate_agent_action and execute_m365_action.
  intent_doc_ref: captured in this plan
  intent_verification: Tasks T1..T7 together satisfy this parent phase's objective.
objective: Add coverage_status to agents.yaml entries; migrate existing data; update validate_agent_action and execute_m365_action.
current_state: Parent phase scope draft; no implementation yet.
target_state: All child tasks complete; GATE green; commit + push to feat/m365-cps-trkC.
scope:
  in_scope:
    - Update agents.yaml schema
    - Migrate existing agents.yaml entries with coverage_status
    - Update validate_agent_action
    - Update execute_m365_action to surface not_yet_implemented
    - Add unit tests
    - Update UCP m365_tools to surface coverage_status
    - Pre-commit + commit
  out_of_scope:
    - Anything outside this parent's allowlist
  file_allowlist:
    - "M365: docs/contracts/agents_yaml_schema.json"
    - "M365: registry/agents.yaml"
    - "M365: src/ucp_m365_pack/client.py"
    - "M365: tests/test_agents_yaml_coverage.py"
    - "M365: tests/test_m365_pack_client.py"
    - "M365: scripts/check_agents_yaml_coverage.py"
    - "UCP: src/ucp/m365_tools.py"
    - "UCP: tests/test_m365_integration.py"
  file_denylist:
    - Files outside coverage_status scope
    - Any file not in allowlist of the active child-phase task
requirements:
  - id: R6
    ref: "plan:m365-cps-trkC-p2-agents-yaml-schema:R6"
    description: Carry-through of master R6; this parent phase contributes scoped behavior.
    acceptance_criteria: Per-task exit criteria; parent GATE passes.
    depends_on:
      - none
    user_intent_trace: Master R6
  - id: R8
    ref: "plan:m365-cps-trkC-p2-agents-yaml-schema:R8"
    description: Carry-through of master R8; this parent phase contributes scoped behavior.
    acceptance_criteria: Per-task exit criteria; parent GATE passes.
    depends_on:
      - none
    user_intent_trace: Master R8
execution_sequence:
  ordering:
    - T1
    - T2
    - T3
    - T4
    - T5
    - T6
    - T7
  stop_on_first_failure: true
  maths_phase_mapping: null
tasks:
  - id: T1
    ref: "plan:m365-cps-trkC-p2-agents-yaml-schema:T1"
    name: Update agents.yaml schema
    description: "Modify schema to allow allowed_actions entries to be either str or {action: str, coverage_status: enum}."
    requirement_refs:
      - R6
    depends_on: []
    maths_phase: null
    deliverables:
      - agents.yaml schema doc updated
    exit_criteria:
      - YAML loads with new structure
    validation_command: "cd $M365_ROOT && .venv/bin/python -c \"import yaml; data=yaml.safe_load(open('registry/agents.yaml')); print('valid')\""
    implementation_notes: null
    files_to_read: []
    files_to_modify:
      - "M365: docs/contracts/agents_yaml_schema.json"
  - id: T2
    ref: "plan:m365-cps-trkC-p2-agents-yaml-schema:T2"
    name: Migrate existing agents.yaml entries with coverage_status
    description: "For each allowed_action: check if action_id is in alias table (status=aliased), in registry directly (status=implemented), or neither (status=planned). Migrate entries."
    requirement_refs:
      - R6
    depends_on:
      - T1
    maths_phase: null
    deliverables:
      - registry/agents.yaml with coverage_status on every action
    exit_criteria:
      - yaml.safe_load passes; every action has coverage_status
    validation_command: "cd $M365_ROOT && .venv/bin/python scripts/check_agents_yaml_coverage.py"
    implementation_notes: null
    files_to_read: []
    files_to_modify:
      - "M365: registry/agents.yaml"
  - id: T3
    ref: "plan:m365-cps-trkC-p2-agents-yaml-schema:T3"
    name: Update validate_agent_action
    description: validate_agent_action returns coverage_status alongside the boolean.
    requirement_refs:
      - R6
    depends_on:
      - T2
    maths_phase: null
    deliverables:
      - src/ucp_m365_pack/client.py with updated function
    exit_criteria:
      - validate_agent_action returns (bool, str, status)
    validation_command: "cd $M365_ROOT && .venv/bin/python -c \"from ucp_m365_pack.client import validate_agent_action; result = validate_agent_action('m365-administrator', 'sites.list'); print(result)\""
    implementation_notes: null
    files_to_read: []
    files_to_modify:
      - "M365: src/ucp_m365_pack/client.py"
  - id: T4
    ref: "plan:m365-cps-trkC-p2-agents-yaml-schema:T4"
    name: Update execute_m365_action to surface not_yet_implemented
    description: "If coverage_status == 'planned', short-circuit: return status_class='not_yet_implemented' without calling runtime."
    requirement_refs:
      - R6
    depends_on:
      - T3
    maths_phase: null
    deliverables:
      - src/ucp_m365_pack/client.py with short-circuit
    exit_criteria:
      - execute_m365_action on planned action returns not_yet_implemented
    validation_command: "cd $M365_ROOT && .venv/bin/python -m pytest tests/test_m365_pack_client.py -k not_yet_implemented -x"
    implementation_notes: null
    files_to_read: []
    files_to_modify:
      - "M365: src/ucp_m365_pack/client.py"
  - id: T5
    ref: "plan:m365-cps-trkC-p2-agents-yaml-schema:T5"
    name: Add unit tests
    description: tests/test_agents_yaml_coverage.py + tests/test_m365_pack_client.py with planned-action behavior.
    requirement_refs:
      - R6
    depends_on:
      - T4
    maths_phase: null
    deliverables:
      - Tests added
    exit_criteria:
      - pytest -k coverage exits 0
    validation_command: "cd $M365_ROOT && .venv/bin/python -m pytest -k coverage -x"
    implementation_notes: null
    files_to_read: []
    files_to_modify:
      - "M365: tests/test_agents_yaml_coverage.py"
      - "M365: tests/test_m365_pack_client.py"
  - id: T6
    ref: "plan:m365-cps-trkC-p2-agents-yaml-schema:T6"
    name: Update UCP m365_tools to surface coverage_status
    description: MCP tool responses include coverage_status when relevant; not_yet_implemented status surfaces clearly.
    requirement_refs:
      - R6
    depends_on:
      - T5
    maths_phase: null
    deliverables:
      - src/ucp/m365_tools.py updated
    exit_criteria:
      - UCP MCP tool returns the new status
    validation_command: "cd $UCP_ROOT && .venv/bin/python -m pytest tests/test_m365_integration.py -k coverage_status -x"
    implementation_notes: null
    files_to_read: []
    files_to_modify:
      - "UCP: src/ucp/m365_tools.py"
      - "UCP: tests/test_m365_integration.py"
  - id: T7
    ref: "plan:m365-cps-trkC-p2-agents-yaml-schema:T7"
    name: Pre-commit + commit
    description: Standard validation + commit.
    requirement_refs:
      - R8
    depends_on:
      - T6
    maths_phase: null
    deliverables:
      - Commit in both repos
    exit_criteria:
      - git log shows commit
    validation_command: "cd $M365_ROOT && pre-commit run --all-files && make test && cd $UCP_ROOT && pre-commit run --all-files && make test && git log --oneline -1 | grep 'plan:m365-cps-trkC-p2'"
    implementation_notes: null
    files_to_read: []
    files_to_modify: []
gates:
  checks:
    - id: "CHECK:C0"
      name: Parent phase C2 integration
      description: All child tasks T1..T7 green; integration test pass
      task_ref: T1..T7
      validation_command: "cd $M365_ROOT && .venv/bin/python tests/integration/m365_coverage_status_e2e.py"
      pass_criteria: Integration test exit code 0
      fail_action: block
  decision:
    go_rule: All gates pass
    no_go_rule: Any gate fails
  no_go_triggers:
    - Any child task fails its validation_command
    - Parent GATE integration test fails
    - File outside parent allowlist touched
determinism: null
artifacts: []
environment:
  python_version: ">=3.11"
  venv: .venv/bin/python
  additional_dependencies: []
  hardware: standard CI; live tenant for GATE smoke tests
  external_data:
    - live M365 tenant (smarthausgroup.com)
  pre_notebook_check: N/A
implementation_approach:
  options_considered:
    - id: A
      description: All-at-once monolithic change
      pros:
        - fewer commits
      cons:
        - drift risk
        - no per-phase gates
    - id: B
      description: Per-track / per-parent / per-child phase hierarchy
      pros:
        - enforced cadence
        - per-task scope fence
      cons:
        - more files
  chosen: B
  rationale: Granular commits enforce cadence; per-task MATHS prompts give executor zero drift opportunity.
  rejected_rationale:
    A: Too coarse; would require massive PR review and lose per-phase gate value
  adr_ref: null
risks:
  items:
    - risk: Cross-repo drift during edits
      impact: medium
      mitigation: Per-task allowlist explicit per repo
      status: mitigated
  hard_blockers: []
rollback:
  procedure:
    - Revert commits on the feature branch.
    - "If parent phase pushed: revert push via new revert commits."
    - Update ACTION_LOG with rollback entry.
    - Set this plan status to cancelled.
  files_to_revert:
    - all files in this plan's allowlist that were modified
  artifacts_to_delete:
    - any generated artifact files for this plan
  governance_updates_on_rollback:
    - ACTION_LOG entry with rollback reason
    - EXECUTION_PLAN revert progress
    - "This plan: status to cancelled"
prompt_references:
  maths_template: agent_governance/master-maths-prompt-template.md
  prompt_doc: null
  prompt_kickoff: null
  per_task_prompts:
    - docs/prompts/codex-m365-cps-trkC-p2-agents-yaml-schema-T1.md
    - docs/prompts/codex-m365-cps-trkC-p2-agents-yaml-schema-T2.md
    - docs/prompts/codex-m365-cps-trkC-p2-agents-yaml-schema-T3.md
    - docs/prompts/codex-m365-cps-trkC-p2-agents-yaml-schema-T4.md
    - docs/prompts/codex-m365-cps-trkC-p2-agents-yaml-schema-T5.md
    - docs/prompts/codex-m365-cps-trkC-p2-agents-yaml-schema-T6.md
    - docs/prompts/codex-m365-cps-trkC-p2-agents-yaml-schema-T7.md
traceability:
  files_created:
    notebooks: []
    invariants: []
    lemmas: []
    code_files: []
    config_artifacts: []
  index_updates:
    traceability_index: null
    invariant_index: null
    math_readme: null
    lemmas_appendix: null
governance_closure:
  action_log: docs/platform/CODDEX_ACTION_LOG.md (UCP) / Operations/ACTION_LOG.md (M365)
  execution_plan: docs/platform/EXECUTION_PLAN.md (UCP) / Operations/EXECUTION_PLAN.md (M365)
  project_status: docs/platform/PROJECT_STATUS.md (UCP)
  invariant_promotions: []
  lemma_promotions: []
execution_outcome:
  task_checklist: []
  gate_checklist: []
  final_decision: null
  approved_by: null
  completion_timestamp: null
when_blocked:
  format: "STATUS: BLOCKED / TASK: {id} / REASON: {desc} / MISSING: {list} / NEXT ALLOWED ACTION: {path}"
  escalation:
    primary: Phil Siniscalchi (Founder/Owner)
    secondary: Engineering lead
agent_constraints:
  - Do not touch files outside Section 5 file allowlist.
  - Do not skip or reorder tasks (Section 7 strict ordering).
  - Do not modify thresholds or invariants to make gates green without owner approval.
  - Do not execute until plan status is approved.
  - "Reference plan:{plan-id}:{task-id} in all action log entries and commits."
  - Do not add tasks, gates, or deliverables not in this plan without owner approval.
  - Use .venv/bin/python for all Python commands.
  - Call validate_action MCP tool before any write operation (UCP server).
  - Each child phase commits locally; no push until parent-phase GATE is green.
  - Each parent-phase GATE pushes to feat/m365-cps-trk{X} branch only.
  - Each track GATE merges to development (UCP) and main (M365) only after live tenant smoke green.
references:
  north_star: docs/NORTH_STAR.md (UCP) / Operations/NORTHSTAR.md (M365)
  execution_plan: docs/platform/EXECUTION_PLAN.md (UCP) / Operations/EXECUTION_PLAN.md (M365)
  applicable_rules:
    - ".cursor/rules/**/*.mdc (M365)"
  related_plans:
    - "plan:m365-cps-trkC-truth-in-advertising"
  related_docs: []
  maths_prompt_template: agent_governance/master-maths-prompt-template.md
_template:
  version: 1.0
  last_updated: 2026-05-01
  schema_json: agent_governance/PLAN_TEMPLATE_SCHEMA.json
