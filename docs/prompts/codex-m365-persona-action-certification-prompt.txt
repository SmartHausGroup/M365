Follow `docs/prompts/codex-m365-persona-action-certification.md`.

Plan reference: `plan:m365-persona-action-certification`.

This program is approved to start. Stay on the current branch, call MCP `validate_action` before any mutating action, execute `G0 -> G5` in order, and stop on first red that escapes the allowlist or requires UCP-side work.

The target is not direct-surface readiness anymore. The target is a truthfully certified persona-to-action workforce graph with no unclassified orphan or stubbed surface.
