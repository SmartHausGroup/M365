# L93 — Persona-Action Legacy-Stub Remediation v4

## Intent

Freeze the bounded fourth `P2` legacy-stub remediation wave so it can be implemented under explicit notebook-backed authority instead of ad hoc project-coordination rewrites.

## Alias Set

- `task.assign`
- `deadline.track`
- `status.update`
- `report.generate`

## Replacement Modes

- `task.assign -> create_persona_task`
- `deadline.track -> list_persona_tasks`
- `status.update -> update_persona_task`
- `report.generate -> build_persona_work_history`

## Deterministic Claim

If the bounded fourth-wave alias set is preserved exactly and each alias routes through the stated internal workforce primitive, the fourth bounded `P2` implementation wave can be validated as a truthful reduction of the legacy-stub surface.
